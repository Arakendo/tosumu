//! `tosumu` command-line interface.
//!
//! Pre-alpha stub. See `DESIGN.md §11` for the roadmap of subcommands.

fn main() {
    eprintln!(
        "{} (pre-alpha) — no subcommands implemented yet. See DESIGN.md.",
        tosumu_core::NAME,
    );
    std::process::exit(2);
}
